apiVersion: v1
kind: Service
metadata:
  name: ingestion-service
  namespace: video-processing
  labels:
    app: ingestion-service
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    name: http
  selector:
    app: ingestion-service
