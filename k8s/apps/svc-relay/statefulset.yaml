apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: svc-relay
  namespace: video-processing
  labels:
    app: svc-relay
spec:
  serviceName: svc-relay
  replicas: 1
  selector:
    matchLabels:
      app: svc-relay
  template:
    metadata:
      labels:
        app: svc-relay
    spec:
      containers:
      - name: svc-relay
        image: svc-relay:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000 # FastAPI
        - containerPort: 8554 # RTSP Stream Port
        envFrom:
        - configMapRef:
            name: svc-relay-config
        env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        volumeMounts:
        - name: video-storage
          mountPath: /tmp
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
      volumes:
      - name: video-storage
        emptyDir: {} # Replace with volumeClaimTemplates for persistence
